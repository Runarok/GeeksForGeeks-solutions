//{ Driver Code Starts

// } Driver Code Ends

#include <iostream>
using namespace std;

int main() {
    int a;
    float b;
    double c;
    long long d;

    cin >> a >> b >> c >> d;
    
    // Print the size of each variable type using sizeof() operator
    printf("%lu %lu %lu %lu \n", sizeof(a), sizeof(b), sizeof(c), sizeof(d));

    return 0;
}



//{ Driver Code Starts.

// } Driver Code Ends